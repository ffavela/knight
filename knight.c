#include<stdio.h>
#include"headers.h"

int main(void)
{
	int tot = ROWS*COLS, mvnum = 1;
	int M[COLS][ROWS];

	while(mvmun != tot){
		for(mchoice = 1; mchoice <= 8; mchoice++)
			move(m, n, (*M)[ROWS], mchoice, mvnum, rev);
	}
}
